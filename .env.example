# PLIMAP 환경변수 템플릿
#
# local 프로필은 compose.yml과 application-local.yml의 로컬 전용 고정값을 사용하므로
# 별도의 .env 설정이 필요하지 않습니다.
# 아래 항목은 추후 dev/prod 환경에서 외부 DB를 연결할 때 사용할 템플릿입니다.
# 실제 비밀번호가 들어가는 .env는 Git에 커밋하지 않습니다.

# PostgreSQL JDBC URL
DB_URL={}
DB_USERNAME={}
DB_PASSWORD={}
